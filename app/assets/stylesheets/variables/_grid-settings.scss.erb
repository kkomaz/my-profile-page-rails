$column-check: <%= Rails.env.production? ? 'hide' : 'show-columns' %>;

$susy: (
  columns: 12,
  column-width: 80px,
);

$susy-phone: (
  columns: 4
);

$susy-debug: (
  debug: (
    image: $column-check,
    color: rgba(#00f, .2),
    output: background
  )
);
